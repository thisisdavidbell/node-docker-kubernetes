apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: hello-node-from-yaml
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: hello-node
    spec:
      containers:
      - name: hello-node
        image: hello-node:v3
        ports:
        - containerPort: 8080
